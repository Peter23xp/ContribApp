/**
 * database.ts — Configuration de la base de données
 * 
 * En mode production, Firestore est utilisé directement.
 * Ce fichier est conservé pour la compatibilité des imports existants.
 */

// Firebase/Firestore est toujours utilisé en production
export const USE_LOCAL_DB = false;
